import { createContext } from "react";


export const Visibility = createContext(false)
export const Coordinate = createContext({})
export const CartContaxt = createContext([])
export const ResInfo = createContext([])